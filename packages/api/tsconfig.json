{
  "extends": "../../tsconfig.compilerOption.json",
  "compilerOptions": {
    "baseUrl": ".",
    "rootDirs": [ // Note! This pattern means, tsc will produce a dist/src directory with all the types
      "src",
      "package.json"
    ],
    "tsBuildInfoFile": "dist/tsconfig.tsbuildinfo",
    "outDir": "dist",
  },
  "include": ["src/**/*", "./package.json"],
  "references": [
    { "path": "../auth" },
  ]
}
